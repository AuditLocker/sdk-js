/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type BatchEventResultSuccess = {
  /**
   * Discriminator literal — narrows the union to the success variant.
   */
  status: "accepted";
  index: number;
  /**
   * Unique identifier for the created event.
   */
  id: string;
  receivedAt: Date;
  /**
   * Field paths that were redacted by data-loss-prevention scanning, if any. Per-event — present on every accepted batch item.
   */
  dlpRedactedFields: Array<string> | null;
};

/** @internal */
export const BatchEventResultSuccess$inboundSchema: z.ZodMiniType<
  BatchEventResultSuccess,
  unknown
> = z.pipe(
  z.object({
    status: types.literal("accepted"),
    index: types.number(),
    id: types.string(),
    received_at: types.date(),
    dlp_redacted_fields: types.nullable(z.array(types.string())),
  }),
  z.transform((v) => {
    return remap$(v, {
      "received_at": "receivedAt",
      "dlp_redacted_fields": "dlpRedactedFields",
    });
  }),
);

export function batchEventResultSuccessFromJSON(
  jsonString: string,
): SafeParseResult<BatchEventResultSuccess, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => BatchEventResultSuccess$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'BatchEventResultSuccess' from JSON`,
  );
}
